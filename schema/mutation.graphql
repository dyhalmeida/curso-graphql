# Pontos de entradas da API
type Mutation {
  createUser(name: String!, lastname: String!, age: Int!, email: String!): User!
  deleteUserById(id: ID!): User
  updateUser(id: ID!, name: String, lastname: String, age: Int, email: String): User
}